package com.lastsave.block03.topic02;

public class QuestB3T2 {

    /**
     * Реализуй метод, который принимает массив строк и возвращает их склейку.
     * <p>Используй {@link StringBuilder}.</p>
     * <p>Примеры:</p>
     * <ul>
     *   <li>{@code join(new String[]{"Hello", "World"})} → {@code "HelloWorld"}</li>
     *   <li>{@code join(new String[]{"a", "b", "c"})} → {@code "abc"}</li>
     * </ul>
     *
     * @param words массив строк
     * @return склейка всех строк
     */
    public String join(String[] words) {
        // TODO: напиши тут свой код
        return null;
    }

    /**
     * Реализуй метод, который склеивает массив строк через разделитель.
     * <p>Используй {@link StringBuilder}.</p>
     * <p>Примеры:</p>
     * <ul>
     *   <li>{@code joinWithSeparator(new String[]{"a", "b", "c"}, ", ")} → {@code "a, b, c"}</li>
     *   <li>{@code joinWithSeparator(new String[]{"Hello", "World"}, " ")} → {@code "Hello World"}</li>
     * </ul>
     *
     * @param words     массив строк
     * @param separator разделитель
     * @return строки, склеенные через разделитель
     */
    public String joinWithSeparator(String[] words, String separator) {
        // TODO: напиши тут свой код
        return null;
    }

    /**
     * Реализуй метод, который возвращает строку, повторённую {@code n} раз.
     * <p>Используй {@link StringBuilder}.</p>
     * <p>Примеры:</p>
     * <ul>
     *   <li>{@code repeat("ab", 3)} → {@code "ababab"}</li>
     *   <li>{@code repeat("x", 5)} → {@code "xxxxx"}</li>
     * </ul>
     *
     * @param s строка для повторения
     * @param n количество повторений
     * @return строка, повторённая n раз
     */
    public String repeat(String s, int n) {
        // TODO: напиши тут свой код
        return null;
    }

    /**
     * Реализуй метод, который возвращает строку в обратном порядке.
     * <p>Используй {@link StringBuilder}.</p>
     * <p>Примеры:</p>
     * <ul>
     *   <li>{@code reverse("Hello")} → {@code "olleH"}</li>
     *   <li>{@code reverse("Java")} → {@code "avaJ"}</li>
     * </ul>
     *
     * @param s входная строка
     * @return строка в обратном порядке
     */
    public String reverse(String s) {
        // TODO: напиши тут свой код
        return null;
    }

    /**
     * Реализуй метод, который строит строку вида {@code "Символов: N"},
     * где N — длина входной строки.
     * <p>Используй {@link StringBuilder}.</p>
     * <p>Примеры:</p>
     * <ul>
     *   <li>{@code charCount("Hello")} → {@code "Символов: 5"}</li>
     *   <li>{@code charCount("")} → {@code "Символов: 0"}</li>
     * </ul>
     *
     * @param s входная строка
     * @return строка вида "Символов: N"
     */
    public String charCount(String s) {
        // TODO: напиши тут свой код
        return null;
    }
}
